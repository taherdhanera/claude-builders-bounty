Generate a structured changelog from the current repository's git history.

Run this command from the repository root:

```bash
bash changelog.sh CHANGELOG.md
```

After it finishes, summarize the generated `CHANGELOG.md` sections and mention the commit range used by the script.
